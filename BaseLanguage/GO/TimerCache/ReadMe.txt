1. 参考cache2go自己写的有时限缓存系统
2. 简要逻辑为每次寻找所有cache中最先会过期的cache然后调整下次垃圾回收的时间,由于该算法为每次检查到该对象过期的时间还有多长,
   保存一个最小长度,然后更新下次执行时间,但是会有小部分误差,就是循环执行的时间.测试了2万个循环花费大概7-8毫秒,因此是否可以
   优化为查找最近过期的确定时间,保存这个确定的时间,而不是保存过期还有多久,第二在每个对象插入的时候按照过期先后顺序排序,维护一个有序队列
   这样不需要遍历所有对象.找到第一个没过期的,把这个时间设置为下次计时器执行的时间.
   
